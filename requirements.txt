# Devflow - Python Dependencies
# Install with: pip install -r requirements.txt
# For development: pip install -r requirements-dev.txt

# Core dependencies (none - using only stdlib)
# The cost tracking system is designed to work with Python stdlib only

# Optional: requests for live currency rates (not required)
# requests>=2.28.0
